<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yasinyuan.testspring.dao.PointsGainRuleMapper">
    <resultMap id="BaseResultMap" type="com.yasinyuan.testspring.model.PointsGainRule">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="business_type" property="businessType" jdbcType="TINYINT" />
        <result column="points" property="points" jdbcType="INTEGER" />
        <result column="daily_limit" property="dailyLimit" jdbcType="INTEGER" />
        <result column="monthly_limit" property="monthlyLimit" jdbcType="INTEGER" />
        <result column="status" property="status" jdbcType="TINYINT" />
        <result column="description" property="description" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="Base_Column_List">
        id, business_type, points, daily_limit, monthly_limit, status, description, create_time, update_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from points_gain_rule
        where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectByBusinessType" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from points_gain_rule
        where business_type = #{businessType,jdbcType=TINYINT}
    </select>

    <select id="selectByStatus" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from points_gain_rule
        where status = #{status,jdbcType=TINYINT}
        order by business_type asc
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from points_gain_rule
        order by business_type asc
    </select>

    <insert id="insert" parameterType="com.yasinyuan.testspring.model.PointsGainRule">
        insert into points_gain_rule (business_type, points, daily_limit, monthly_limit, status, description)
        values (#{businessType,jdbcType=TINYINT}, #{points,jdbcType=INTEGER}, #{dailyLimit,jdbcType=INTEGER}, #{monthlyLimit,jdbcType=INTEGER}, #{status,jdbcType=TINYINT}, #{description,jdbcType=VARCHAR})
    </insert>

    <insert id="insertSelective" parameterType="com.yasinyuan.testspring.model.PointsGainRule">
        insert into points_gain_rule
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="businessType != null">business_type,</if>
            <if test="points != null">points,</if>
            <if test="dailyLimit != null">daily_limit,</if>
            <if test="monthlyLimit != null">monthly_limit,</if>
            <if test="status != null">status,</if>
            <if test="description != null">description,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="businessType != null">#{businessType,jdbcType=TINYINT},</if>
            <if test="points != null">#{points,jdbcType=INTEGER},</if>
            <if test="dailyLimit != null">#{dailyLimit,jdbcType=INTEGER},</if>
            <if test="monthlyLimit != null">#{monthlyLimit,jdbcType=INTEGER},</if>
            <if test="status != null">#{status,jdbcType=TINYINT},</if>
            <if test="description != null">#{description,jdbcType=VARCHAR},</if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.yasinyuan.testspring.model.PointsGainRule">
        update points_gain_rule
        <set>
            <if test="businessType != null">business_type = #{businessType,jdbcType=TINYINT},</if>
            <if test="points != null">points = #{points,jdbcType=INTEGER},</if>
            <if test="dailyLimit != null">daily_limit = #{dailyLimit,jdbcType=INTEGER},</if>
            <if test="monthlyLimit != null">monthly_limit = #{monthlyLimit,jdbcType=INTEGER},</if>
            <if test="status != null">status = #{status,jdbcType=TINYINT},</if>
            <if test="description != null">description = #{description,jdbcType=VARCHAR},</if>
            update_time = CURRENT_TIMESTAMP,
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.yasinyuan.testspring.model.PointsGainRule">
        update points_gain_rule
        set business_type = #{businessType,jdbcType=TINYINT},
            points = #{points,jdbcType=INTEGER},
            daily_limit = #{dailyLimit,jdbcType=INTEGER},
            monthly_limit = #{monthlyLimit,jdbcType=INTEGER},
            status = #{status,jdbcType=TINYINT},
            description = #{description,jdbcType=VARCHAR},
            update_time = CURRENT_TIMESTAMP
        where id = #{id,jdbcType=BIGINT}
    </update>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from points_gain_rule
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <delete id="deleteByBusinessType" parameterType="java.lang.Integer">
        delete from points_gain_rule
        where business_type = #{businessType,jdbcType=TINYINT}
    </delete>

    <delete id="deleteByStatus" parameterType="java.lang.Integer">
        delete from points_gain_rule
        where status = #{status,jdbcType=TINYINT}
    </delete>

    <select id="countByBusinessType" parameterType="java.lang.Integer" resultType="java.lang.Integer">
        select count(*) from points_gain_rule where business_type = #{businessType,jdbcType=TINYINT}
    </select>

    <select id="countByStatus" parameterType="java.lang.Integer" resultType="java.lang.Integer">
        select count(*) from points_gain_rule where status = #{status,jdbcType=TINYINT}
    </select>
</mapper>